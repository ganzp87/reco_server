type AppleGetAppResponse {
	appleApp: [AppleApp]
	error: String
}

type Query {
	AppleGetApp(
		language: String!
		country: String!
		category: String!
		searchCountry: String!
		dataCount: Int!
	): AppleGetAppResponse!
}
