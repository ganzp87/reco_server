type GoogleSaveSearchResponse {
	googleApp: [GoogleApp]
	error: String
}

type Query {
	GoogleSaveSearch(
		searchWords: String!
		language: String!
		country: String!
		category: String!
	): GoogleSaveSearchResponse!
}
