type GoogleGetSearchResponse {
	googleApp: [GoogleApp]
	error: String
}

type Query {
	GoogleGetSearch(
		searchWords: String!
		language: String!
		country: String!
		category: String!
	): GoogleGetSearchResponse!
}
