type GoogleGetAppResponse {
	googleApp: [GoogleApp]
	error: String
}

type Query {
	GoogleGetApp(
		language: String!
		searchCountry: String!
		category: String!
		myLanguage: String!
		dataCount: Int!
	): GoogleGetAppResponse!
}
